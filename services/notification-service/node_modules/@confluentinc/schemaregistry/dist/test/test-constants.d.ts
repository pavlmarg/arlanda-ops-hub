import { ClientConfig } from '../rest-service';
declare const clientConfig: ClientConfig;
declare const mockClientConfig: ClientConfig;
declare const mockTtlClientConfig: ClientConfig;
declare const maxRetries = 2;
declare const retriesWaitMs = 100;
declare const retriesMaxWaitMs = 1000;
export { clientConfig, mockClientConfig, mockTtlClientConfig, maxRetries, retriesWaitMs, retriesMaxWaitMs };
