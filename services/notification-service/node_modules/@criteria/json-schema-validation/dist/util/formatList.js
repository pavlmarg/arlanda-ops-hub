"use strict";
exports.__esModule = true;
exports.formatList = void 0;
function formatList(values, conjunction) {
    if (values.length === 0) {
        return '';
    }
    if (values.length === 1) {
        return values[0];
    }
    var commaSeparatedValues = values.slice(0, -1).join(', ');
    var finalValue = values[values.length - 1];
    return "".concat(commaSeparatedValues, " ").concat(conjunction, " ").concat(finalValue);
}
exports.formatList = formatList;
