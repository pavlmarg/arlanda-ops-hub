"use strict";
exports.__esModule = true;
exports.booleanValidator = void 0;
var format_1 = require("../util/format");
function booleanValidator(schema, schemaPath, _a) {
    var outputFormat = _a.outputFormat;
    var schemaLocation = schemaPath.join('');
    if (schema) {
        return function (instance, instanceLocation) { return ({ valid: true, schemaLocation: schemaLocation, instanceLocation: instanceLocation }); };
    }
    else {
        return outputFormat === 'flag'
            ? function (instance, instanceLocation) { return ({ valid: false }); }
            : function (instance, instanceLocation) { return ({
                valid: false,
                schemaLocation: schemaLocation,
                instanceLocation: instanceLocation,
                message: "should not be defined but is ".concat((0, format_1.format)(instance))
            }); };
    }
}
exports.booleanValidator = booleanValidator;
