import type { JSONPointer } from '@criteria/json-pointer';
import { JSONSchemaObject } from '@criteria/json-schema/draft-06';
import { Output } from '../../../../validation/Output';
import { ValidatorContext } from '../../../../validation/keywordValidators';
export declare function exclusiveMaximumValidator(schema: JSONSchemaObject, schemaPath: JSONPointer[], context: ValidatorContext): (instance: any, instanceLocation: JSONPointer, annotationResults: Record<string, any>) => Output;
