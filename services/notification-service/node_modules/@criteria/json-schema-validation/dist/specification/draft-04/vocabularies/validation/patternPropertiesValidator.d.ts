import type { JSONPointer } from '@criteria/json-pointer';
import { JSONSchema } from '@criteria/json-schema/draft-04';
import { InvalidVerboseOutput, Output } from '../../../../validation/Output';
import { ValidatorContext } from '../../../../validation/keywordValidators';
export declare function patternPropertiesValidator(schema: JSONSchema, schemaPath: JSONPointer[], context: ValidatorContext): (instance: any, instanceLocation: JSONPointer, annotationResults: Record<string, any>) => Output;
export declare function formatMessage(errors: InvalidVerboseOutput[] | null, invalidPropertyNames: string[]): any;
