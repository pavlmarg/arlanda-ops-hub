import type { JSONPointer } from '@criteria/json-pointer';
import { JSONSchema } from '@criteria/json-schema/draft-04';
import { Output } from '../../../../validation/Output';
import { ValidatorContext } from '../../../../validation/keywordValidators';
export declare function notValidator(schema: JSONSchema, schemaPath: JSONPointer[], context: ValidatorContext): (instance: unknown, instanceLocation: JSONPointer, annotationResults: Record<string, any>) => Output;
export declare function formatMessage(): string;
