import { $dynamicRefValidator } from './$dynamicRefValidator';
import { $refValidator } from './$refValidator';
export declare const coreValidators: {
    $ref: typeof $refValidator;
    $dynamicRef: typeof $dynamicRefValidator;
};
