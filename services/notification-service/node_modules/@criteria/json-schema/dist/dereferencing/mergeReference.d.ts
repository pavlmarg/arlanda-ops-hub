import { ReferenceInfo } from '../schema-index/types';
import { URI } from '../util/uri';
export type ReferenceMergePolicy = 'by_keyword' | 'overwrite' | 'none' | 'default';
export declare function mergeReference<Metadata extends {
    metaSchemaID: URI;
}>(reference: {
    $ref: string;
}, info: ReferenceInfo<Metadata>, dereferencedValue: any, referenceMergePolicy: ReferenceMergePolicy): {};
