import { DocumentIndex } from '../schema-index/DocumentIndex';
import { ReferenceInfo } from '../schema-index/types';
import { URI } from '../util/uri';
export declare function dereferenceReferences<Metadata extends {
    metaSchemaID: URI;
}>(rootObject: any, references: Map<object, ReferenceInfo<Metadata>>, index: DocumentIndex, merge: (reference: {
    $ref: string;
}, info: ReferenceInfo<Metadata>, dereferencedValue: any) => object): void;
